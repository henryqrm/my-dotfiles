#!/bin/bash
cd ~
#
# SSH
#
git clone https://henryqrm@bitbucket.org/henryqrm/.ssh.git

# ssh-keygen -t rsa -b 4096 -C "henryqrm@gmail.com"
eval "$(ssh-agent -s)"
chmod 400 ~/.ssh/id_rsa
ssh-add ~/.ssh/id_rsa

#
# 
#
git init
git remote add origin git@github.com:henryqrm/my-dotfiles.git
git pull origin master


sudo usermod -s /usr/bin/zsh $USER

sudo pacman -S yarn npm
yay -S boostnote unclutter newaita-icons-git google-chrome firefox-developer-edition android-sdk-platform-tools gitflow-avh nodejs nvm virtualbox virtualbox-guest-modules-arch virtualbox-guest-modules-arch docker docker-compose --noconfirm
sudo npm i -g npm n http-serve concurrently jest nock mocha

git config --global user.email "henryqrm@gmail.com"
git config --global user.name "Henrique Rodrigues"

echo fs.inotify.max_user_watches=524288 | sudo tee /etc/sysctl.d/40-max-user-watches.conf && sudo sysctl --system
