#!/bin/bash

sudo pacman -S yarn
yay -S boostnote unclutter newaita-icons-git google-chrome firefox-developer-edition android-sdk-platform-tools gitflow-avh nodejs nvm npm virtualbox virtualbox-guest-modules-arch virtualbox-guest-modules-arch docker docker-compose --noconfirm
sudo npm i -g npm n http-serve concurrently jest nock mocha

git config --global user.email "henryqrm@gmail.com"
git config --global user.name "Henrique Rodrigues"

echo fs.inotify.max_user_watches=524288 | sudo tee /etc/sysctl.d/40-max-user-watches.conf && sudo sysctl --system

# ssh-keygen -t rsa -b 4096 -C "henryqrm@gmail.com"
eval "$(ssh-agent -s)"
chmod 400 ~/.ssh/id_rsa
ssh-add ~/.ssh/id_rsa
